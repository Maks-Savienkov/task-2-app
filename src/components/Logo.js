import React from "react";

const Logo = (props) => {
    return <a href="/home" className="logo-link">
            <img className="logo-pic" src="img/svg/logo/logo.svg" alt="{Finsweet"/>
        </a>
}

export default Logo;